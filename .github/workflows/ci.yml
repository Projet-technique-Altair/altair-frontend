name: Frontend CI

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  ci:
    uses: Projet-technique-Altair/altair-ci/.github/workflows/frontend.yml@v1
    with:
      node_version: "20"